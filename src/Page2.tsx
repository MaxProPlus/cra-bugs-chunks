import React from 'react'
import Button from './Button'
import './Page2.scss'

const Page2: React.FC<any> = (p) => {
  return (
    <div>
      page2
      <Button className="btn-page2">Кнопка</Button>
    </div>
  )
}

export default Page2
